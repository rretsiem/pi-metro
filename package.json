{
  "name": "pi-metro",
  "version": "0.2.0",
  "description": "Inter-session message bus for the Pi coding agent. Live Pi sessions discover each other, exchange chat, fixed queries, and context-aware asks.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "keywords": [
    "pi-package",
    "metrol",
    "agent",
    "ipc",
    "bus"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.19.0"
  },
  "peerDependencies": {
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
